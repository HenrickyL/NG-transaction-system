# NG.CASH - Front

* React
* typescript
* tailwind css
* storybook doc


* build docker here:
```sh
docker build -f Dockerfile.dev -t multiserver .
```
* run:
```
docker run -it -p 4003:5000 multiserver
```